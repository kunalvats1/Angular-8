export class Order {
    selectedCategory: string;
    selectedProduct: string;
    selectedPrice: number;
    selectedQuantity: number;
    price: number;
    quantity: number;
    products: string[] = [];
    category: string[] = [];
}